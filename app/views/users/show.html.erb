<!--User infoの部分-->
<%= render 'books/list' %>

<!--Booksの部分-->
<h2>Books</h2>
  <%#<% @books.each do |book| %>
    <div>
      <p><%= link_to user_path(@book.id) do %>
         <%= image_tag current_user.get_profile_image(100, 100) %>
         <% end %>
      </p>

      <p>title : <%= link_to book_path(@book.id) do %>
         <%= @book.title %>
         <% end %>
      </p>
      <p>opinion：<%= @book.body %></p>
    </div>
  <%#<% end %>

<!--New Bookの部分-->
<h2>New Book</h2>
  <%= form_with model: @book do |f| %>
    <h4>title</h4>
      <%= f.text_field :title %>
    <h4>opinion</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Creat Book' %>
  <% end %>

